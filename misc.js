$("#target").empty();  //clears child elements of target?
$("#target").prepend(); // add before (within the target tag)
$("#target").append(); // add after (within the target tag)
$("#target").before(); // add before (outside the target tag)
$("#target").after(); // add after (outside the target tag)

